<ul>
	<% @all_trending.each do |r| %>
		<li>
			<a href="https://github.com/<%= r.name %>" target="_blank"><%= r.name %></a>
			<small>
				<%= r.lang %>
				&mdash;
				<%= r.all_star_count %> &starf;
			</small>
			<br/>
			<%= r.description %>
		</li>
	<% end %>
</ul>
